
<div class="container mt-3">
  <div class="row">
    <div class="col-6 mx-auto">
      <h2><%= @article.title %></h2>
      <p><%= @article.content %></p>
      <%= link_to "Edit ", edit_article_path, class: 'btn btn-primary m-2'  %>
      <%= link_to "Delete", article_path(@article), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%>
    </div>
  </div>
</div>
